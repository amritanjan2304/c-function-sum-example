# c-function-sum-example
A simple C program demonstrating the use of user-defined functions. This project shows how to create a sum() function, pass arguments to it, and reuse the function multiple times with different values.
